package com.pool.util.LOCALE;

import java.util.Locale;

public class LocateClient {
	public static void main(String[] args) {
		Locale locale = new Locale("EN", "India");
		System.out.println(locale);
	}
}
